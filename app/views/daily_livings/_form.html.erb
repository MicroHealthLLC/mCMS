
<fieldset>
  <legend></legend>
  <%= form_for(daily_living, html: { class: 'form-horizontal',  :multipart => true}) do |f| %>
      <% if daily_living.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(daily_living.errors.count, "error") %> prohibited this daily living from being saved:</h2>

            <ul>
              <% daily_living.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <%= f.hidden_field :user_id %>
      <div class="form-group">
        <%= f.label t(:title), class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_field :title , class: 'form-control' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label t(:daily_living_type), class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.select :daily_living_type_id, options_for_select(DailyLivingType.active.pluck(:name, :id), selected: f.object.daily_living_type.try(:id)) , {} ,{class: 'form-control'} %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label t(:daily_living_status), class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.select :daily_living_status_id, options_for_select(DailyLivingStatus.active.pluck(:name, :id), selected: f.object.daily_living_status.try(:id)) , {} ,{class: 'form-control'} %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label t(:description) , class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_area :description , class: 'form-control ck-editor' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label t(:date_start) , class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_field :date_start , class: 'form-control date_picker' %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label t(:date_end) , class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_field :date_end , class: 'form-control date_picker' %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-md-12">
          <%= f.label t(:attachments) , class: 'col-md-2 control-label' %>
        </div>
        <%= f.simple_fields_for :daily_living_attachments do |attachment| %>
            <%= render 'attachments/attachment_show', :f => attachment %>
        <% end %>
        <div id="attachment_fields" class="col-md-2 margin-top-10 " style="padding-right: 2px;" >
          <%= link_to_add_association 'add file', f, :daily_living_attachments, class: 'btn btn-success pull-right', partial: 'attachments/attachment_fields' %>
        </div>
      </div>

      <div class="form-group" >
        <div class="col-md-11">
          <%= link_to t('button_cancel'), daily_livings_path, class: 'btn btn-warning pull-right' %>
          <%= f.submit t(:button_save), class: 'btn btn-primary pull-right' %>
        </div>
      </div>
  <% end %>
</fieldset>
