<% modules = ['educations', 'other_skills',
              'certifications', 'clearances', 'positions',
              'injuries', 'worker_compensations', 'job_applications',
              'resumes', 'military_histories'
]
%>
<% if can_view_submenu?(modules) %>
    <% if !User.current.allowed_to?(:manage_roles) %>
        <li>
          <a href="#" id="ul_occupation_history"><i class="fa fa-lg fa-fw fa-user"></i> <span class="menu-item-parent">Occupational History</span></a>
          <ul id="occupation_history_menu">

            <% if module_enabled?( 'occupational_record') %>
                <li class="<%= menu_active?('profile_record') %> ">
                  <a href= "/occupational_record" title="Occupational record"><i class="fa fa-lg fa-fw fa-user"></i> <span class="menu-item-parent">Occupational record</span></a>
                </li>
            <% end %>
          </ul>
        </li>
    <% end %>
<% end %>
