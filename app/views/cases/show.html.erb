<div class="col-xs-12 btn-edition">
  <%= link_to('Delete', case_path(@case), class: 'btn btn-danger pull-right',
              :method => :delete,
              :data => {:confirm => t(:text_are_you_sure)}) if @case.owner? or can?(:manage_roles, :delete_cases)
  %>
  <%= link_to(t('button_edit'), edit_case_path(@case), class: 'btn btn-success pull-right') if @case.owner? or can?(:manage_roles, :edit_cases) %>
  <%= link_to(t('button_cancel'), case_back_url(@case), class: 'btn btn-warning pull-right') %>
  <%= link_to(t('button_print'), case_path(@case, format: 'pdf'), class: 'btn btn-primary pull-right') %>
  <%#= link_to('Add relation', new_relation_case_path(@case), class: 'btn btn-primary pull-right') if can?(:manage_roles, :add_cases)  %>
  <%= link_to 'Unlink', delete_sub_case_relation_case_path(@case), class: 'btn btn-primary pull-right' if @case.subcase_id %>
</div>

<section id="widget-grid" class="checklists_account">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'show',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                         wid_id: 'info',
                         wid_title: t('case')
                 } %>
    </article>
  </div>
</section>



<%= render partial: 'cases/show_case/subcases' %>
<%= render partial: 'cases/show_case/notes' %>
<%= render partial: 'cases/show_case/needs' %>
<%= render partial: 'cases/show_case/goals' %>
<%= render partial: 'cases/show_case/plans' %>
<%= render partial: 'cases/show_case/tasks' %>
<%= render partial: 'cases/show_case/documents' %>
<%= render partial: 'cases/show_case/checklists' %>
<%= render partial: 'cases/show_case/appointments' %>
<%= render partial: 'cases/show_case/surveys' %>
<%= render partial: 'cases/show_case/watcher' %>
