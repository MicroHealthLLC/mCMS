<h1>Cases</h1>

<%= link_to('New case', new_case_path, class: 'btn btn-primary pull-right margin-bottom-10') unless (User.current.allowed_to?(:manage_roles) and session[:employee_id].nil?) %>
<%= render 'list' %>
<%= will_paginate @cases, renderer: BootstrapPagination::Rails %>
