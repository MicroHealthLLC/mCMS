<%= link_to( t('new_unit'), new_unit_path, class:'btn btn-primary pull-right margin-bottom-10') if can?(:create_units, :manage_roles, :manage_units) %>
<section id="widget-grid" class="units_section">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'unit',
                     wid_title: t('units')
                 } %>
    </article>
  </div>
</section>
