<%= link_to( t('new_deployment_history'), new_deployment_history_path, class:'btn btn-primary pull-right margin-bottom-10') if can?(:create_deployment_histories, :manage_roles, :manage_deployment_histories) %>
<section id="widget-grid" class="deployment_histories_section">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'deployment_history',
                     wid_title: t('deployment_histories')
                 } %>
    </article>
  </div>
</section>
