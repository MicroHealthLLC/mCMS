<%= link_to(t('new_medication'), new_medication_path, class:'btn btn-primary pull-right margin-bottom-10') if can?(:create_medications, :manage_roles, :manage_medications) %>
<section id="widget-grid" class="medications_section">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'medication',
                     wid_title: t('medications')
                 } %>
    </article>
  </div>
</section>
