<div class="col-xs-12 btn-edition">
  <%= link_to('Delete', case_watcher_path(@case_watcher), class: 'btn btn-danger pull-right',
              :method => :delete,
              :data => {:confirm => t(:text_are_you_sure)}) if can?(:manage_roles, :manage_watchers)
  %>
  <%= link_to(t('button_edit'), edit_case_watcher_path(@case_watcher), class: 'btn btn-success pull-right') if can?(:manage_roles, :manage_watchers) %>
  <%= link_to(t('button_cancel'), case_url(@case_watcher.case), class: 'btn btn-warning pull-right') %>
 </div>