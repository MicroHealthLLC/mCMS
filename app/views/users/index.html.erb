<h1><%= t(:users) %></h1>

<%= link_to t(:new_user), new_employee_path, class: 'btn btn-primary pull-right margin-bottom-10' %>
<table class="table table-bordered table-responsive" id="users_table">
  <thead>
  <tr>
    <th>#</th>
    <th><%= t :login %></th>
    <th><%= t :email %></th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>

  </tbody>
</table>

<script>
  $(document).ready(function(){
    var table = $('#users_table').DataTable({
      "ajax": "<%= users_path(format: :json) %>",
      processing: true,
      serverSide: true,
      responsive: true,
      display: [[0, 'asc']],
      "bDestroy": true,
      "columns": [
        { "searchable": true, "orderable": true },
        { "searchable": true, "orderable": true },
        { "searchable": true, "orderable": true },

        { "searchable": false, "orderable": false },
        { "searchable": false, "orderable": false }
      ],
      "sDom": "<'dt-toolbar'" +
      "<'col-sm-5 col-xs-8'f>" + //search box
      "<'col-sm-2 col-sm-offset-4 col-xs-2 'C>"+// drop down size
      "<'col-sm-1 col-xs-2 'l>>"+// drop down size
      "t"+ // the table
      "<'dt-toolbar-footer'<'col-sm-6 col-xs-12 hidden-xs'i><'col-xs-12 col-sm-6'p>>",
      "iDisplayLength": 10
    })
  } );
</script>