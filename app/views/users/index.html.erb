<%= link_to t(:new_user), new_employee_path, class: 'btn btn-primary pull-right margin-bottom-10' %>
<%= link_to 'Active users', active_users_path, class: 'btn btn-primary pull-right margin-bottom-10' %>
<%= link_to 'Audit users', audit_users_path, class: 'btn btn-primary pull-right margin-bottom-10' %>
<section id="widget-grid" class="users_list">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'users',
                     wid_title: t(:users)
                 } %>
    </article>
  </div>
</section>
