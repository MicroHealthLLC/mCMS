<%#= link_to 'Print', params.permit(:from, :to, :status_type).merge(format: 'pdf'), class: 'pull-right btn btn-primary'  %>
<section id="widget-grid" class="job_applications_section">
  <%= render partial: 'state_button_filter_with_remote', locals: {controller_name: 'job_apps', container: 'job_apps_container'} %>
  <div class="btn-group pull-right btn-edition">
    <button class="btn btn-primary">
      Add Enrollment For
    </button>
    <button class="btn btn-primary dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
      <span class="caret"></span>
    </button>
    <ul class="dropdown-menu">
      <% @cases_for_btn.each do |c| %>
          <li>
            <%= link_to c, new_job_app_path(case_id: c.id) %>
          </li>
      <% end %>
    </ul>
  </div>
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'job_application',
                     wid_title: t('job_applications')
                 } %>
    </article>
  </div>
</section>
