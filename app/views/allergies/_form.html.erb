<fieldset>
  <legend></legend>
  <%= form_for(allergy, html: { class: 'form-horizontal',  :multipart => true}) do |f| %>
      <% if allergy.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(allergy.errors.count, "error") %> prohibited this problem list from being saved:</h2>

            <ul>
              <% allergy.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <%= f.hidden_field :user_id %>

      <div class="form-group">
        <%= f.label t(:icdcm_code), class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= text_field_tag :icdcm_code, f.object.icdcm_code,  class: 'form-control' %>
          <%= f.hidden_field :allergy_type_id %>
          <script>
            $(document).ready(function(){
              $('#icdcm_code').autocomplete({
                source: "/idcdata.json?from=z00",
                minLength: 2,
                select: function( event, ui ) {
                  $('#allergy_icdcm_code_id').val(ui.item.id)
                }
              })
            })
          </script>
        </div>
      </div>
      
    <%#= f.input :medication %>

      <div class="form-group">
        <%= f.label :allergy_date , class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_field :allergy_date, class: 'form-control date_picker' %>
        </div>
      </div>
      
      <div class="form-group">
        <%= f.label t(:allergy_status), class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.select :allergy_status_id, options_for_select(BehavioralRiskStatus.active.pluck(:name, :id), selected: f.object.allergy_status.try(:id)) , {} ,{class: 'form-control'} %>
        </div>
      </div>
      
      <div class="form-group">
        <%= f.label t(:description) , class: 'col-md-2 control-label' %>
        <div class="col-md-9">
          <%= f.text_area :description , class: 'form-control ck-editor' %>
        </div>
      </div>

      <div class="form-group">
        <div class="col-md-12">
          <%= f.label t(:attachments) , class: 'col-md-2 control-label' %>
        </div>
        <%= f.simple_fields_for :allergy_attachments do |attachment| %>
            <%= render 'attachments/attachment_show', :f => attachment %>
        <% end %>
        <div id="attachment_fields" class="col-md-2 margin-top-10 " style="padding-right: 2px;" >
          <%= link_to_add_association 'add file', f, :allergy_attachments, class: 'btn btn-success pull-right', partial: 'attachments/attachment_fields' %>
        </div>
      </div>

      <div class="form-group" >
        <div class="col-md-11">
          <%= link_to t('button_cancel'), allergies_path, class: 'btn btn-warning pull-right' %>
          <%= f.submit t(:button_save), class: 'btn btn-primary pull-right' %>
        </div>
      </div>
  <% end %>
</fieldset>