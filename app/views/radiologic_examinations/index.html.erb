<%= link_to 'Print', params.permit(:from, :to, :status_type).merge(format: 'pdf'), class: 'pull-right btn btn-primary'  %>
<%= link_to(t('new_radiologic_examination'), new_radiologic_examination_path, class:'btn btn-primary pull-right margin-bottom-10') if can?(:create_laboratory_examinations, :manage_roles, :manage_laboratory_examinations) %>

<section id="widget-grid" class="laboratory_examinations_section">
  <%= render partial: 'state_button_filter'%>
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'radiologic_examination',
                     wid_title: t('radiologic_examinations')
                 } %>
    </article>
  </div>
</section>
