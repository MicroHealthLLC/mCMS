
<table class="table table-bordered table-responsive" id="appointments_table">
  <thead>
  <tr>
    <% if params[:controller] != 'cases' %><th>Client</th> <% end %>
    <th>Title</th>
    <th>Appointment type</th>
    <th>Appointment status</th>
    <th>Date</th>
  </tr>
  </thead>

  <tbody>
  <% @appointments.each do |appointment| %>
      <tr>
        <% if params[:controller] != 'cases' %>   <td><%= appointment.user %></td>    <% end %>
        <td><%= link_to appointment.title, appointment_path(appointment) %></td>
        <td><%= appointment.appointment_type %></td>
        <td><%= appointment.appointment_status %></td>
        <td><%= appointment.date %></td>
      </tr>
  <% end %>
  </tbody>
</table>