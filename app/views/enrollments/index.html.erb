<%= link_to( t('new_enrollment'), new_enrollment_path, class:'btn btn-primary pull-right margin-bottom-10') if can?(:create_enrollments, :manage_roles, :manage_enrollments) %>

<section id="widget-grid" class="enrollments_section">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                         enrollments: @enrollments,
                         wid_id: 'enrollments',
                         wid_title: t('enrollments')
                 } %>
    </article>
  </div>
</section>