<%# @param post [Thredded::PostView] %>
<header>
  <h2 class="post--user"><%= user_link post.user %></h2>
  <%= image_tag post.user.profile_image, class: 'img-circle', size: '40x40' if post.user %>
  <p class="post--created-at"><%= time_ago post.created_at %></p>
</header>
