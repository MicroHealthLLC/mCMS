<%= link_to( 'New Lesson', lms.new_course_lesson_path(@course), class:'btn btn-primary pull-right margin-bottom-10') if can?(:manage_roles) %>
<section id="widget-grid" class="allergies_section">
  <div class="row">
    <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
      <%= render partial: 'list',
                 layout: 'shared/smart_widget_wrapper',
                 locals: {
                     wid_id: 'lessons',
                     wid_title: 'Lessons'
                 } %>
    </article>
  </div>
</section>
